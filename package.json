{
  "name": "react-node-template",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "cd src/server && npm install && npm start",
    "dev": "concurrently \"cd src/server && nodemon\" \"cd src/client && npm start\"",
    "package": "nexe -i src/server/app.js -t node12-win-x64 -p /bin/nexe -t node12-linux-x64, node12-macos-x64, node12-win-x64 -n fonts-sync-nexe"
  },
  "bin": "src/server/bin/www",
  "pkg": {
    "scripts": [
      "src/client/build/**/*.js"
    ],
    "assets": [
      "src/client/**/*"
    ]
  },
  "dependencies": {},
  "engines": {
    "node": ">=12.16.1",
    "npm": ">=6.13.4"
  },
  "devDependencies": {}
}
